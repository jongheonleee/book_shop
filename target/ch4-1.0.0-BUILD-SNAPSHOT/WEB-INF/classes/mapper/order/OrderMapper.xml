<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fastcampus.ch4.dao.OrderMapper">
    <insert id="creaetOrder" parameterType="OrderDto">
        INSERT INTO
            order_table
        (
            id,
            created_at,
            delivery_fee,
            total_prod_pric,
            total_bene_pric,
            total_ord_pric,
            reg_date,
            reg_id,
            up_date,
            up_id
        )
        VALUES
        (
            #{id},
            now(),
            #{deliveryFee},
            #{totalProdPric},
            #{totalBenePric},
            #{totalOrd},
            now(),
            #{regId},
            now(),
            #{upId}
        )
    </insert>
</mapper>